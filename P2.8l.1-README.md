# P2.8l.1 — Audit mécanique corrigé

Patch-only, audit uniquement.

Corrections :
- compte correctement `system.actions` lorsqu'il est stocké par Foundryborne
  comme collection/objet indexé ;
- ajoute la correspondance vérifiée :
  `srd-2.0.domain-card.dread.horror` → `Summon Horror` ;
- ajoute les compteurs `matchedWithAnyNativeMechanics` et
  `matchedWithBothNativeMechanics`.

Aucune carte n'est modifiée.

Validation :

```js
const api = game.modules.get("daggerheart-campaign-toolkit").api;
const report = await api.auditNativeDomainCardMechanics();
report;
```

Attendu pour le matching : 210 matched, 0 unmatched, 0 ambiguous,
`greenForReconstruction: true`.

Renvoyer le tableau résumé : les nouveaux nombres d'actions/effects détermineront
la passe de reconstruction suivante.
